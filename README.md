# Netbula

## Yet another container orchestrator

## Where to start

- Build: `go build -o bin/manager ./cmd/manager & go build -o bin/worker ./cmd/worker`

- Run the manager program: `./bin/manager`
- Run the worker program: `./bin/worker`
